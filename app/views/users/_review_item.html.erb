<li id="<%= review_item.id %>">
	<div class="row">
	        <div class="span4">
	       <%= link_to review_item.name, user_review_path(user_id: @user.id, id: review_item.id) %>
	        </div>
	        <div class="span4 timestamp">
	        <%=review_item.created_at.strftime("%B %d, %Y")%>
	        <%# link_to "Delete",
                    user_file_path(:user_id => @user.id, :id => file_item.id),
                    :method => 'delete',
                    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                    :class => 'btn btn-danger' %>
	        </div>
	      
	</div>
</li>